A.
给定两串长度最多为100000的两个字符串，每个字符串都是由单词和空格组成的。要求把第一个字符串中的一些字符删除为空格，使两个字符串相等。两个字符串相等的条件是，把首尾的空格删除，和中间大于一个的空格变为一个空格后，判断两个字符串是否一样。
DP+KMP 对于第二个串的每一个单词，都要在第一个串中找到与之连续且一样的单词，且与之前匹配的单词中间要相差一个字符，不然的话会与之前匹配的单词没有空格，两个串就不会相等了。所以应该在第一个串与之前匹配的单词之后的第二个字符开始匹配KMP当前的单词，如果有多个位置匹配当前单词的话，应该选取最近匹配，因为这样能为后面的单词匹配留下跟多的空间。

B.

C.

D.

E.

F.
给定一个n<=20000，要求用a，b，c三个字符构造出一个字符串，他的任意大于二的字串，都不为回文串。
回文串+枚举 回文串构造上也有递归的特性，如果当前字符不与前一个字符相同，且不与之前第二个字符相同，那么就没办法也他之前的字符为中心构造回文串。

G.

H.

I. 
给定一段长度为l公里的路程，已知一个人的运动为h小时，k公里。求完成长度为l的距离的最小值和最大值。
贪心 如果l不能整除k的话，那么最小值就为不算多余的路程，看做多余的路程的速度无穷大，之后用h小时走完k公里剩下的；最大值把多余的路程要求h小时走完，k公里剩下的无穷大的速度走完。

J.
给定m（m<=10^4）个升级包，要求从1升级到n（1<=n<=10^4），有盗版软件、破解软件、正版软件之分，盗版软件能升级正版软件，正版软件不能升级盗版软件，破解软件能升级正版和盗版软件。如果被盗版软件升级后，以后所以的软件都是盗版的。要求最小的下载量，能升级到版本n。
DP f[n][2]表示升级到n级，为正版或盗版需要的最小下载量。
